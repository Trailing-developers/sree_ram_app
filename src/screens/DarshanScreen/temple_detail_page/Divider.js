import React from "react";
import { View } from "react-native";

const Divider = () => {
  return (
    <View
      style={{
        borderBottomColor: "gray",
        borderBottomWidth: 1,
      }}
    />
  );
};

export default Divider;
